# 航空状态栏转 MVU：第一张实卡样本

> 当前结果：导入、初始化、真实后台结算已通过；自动刷新缺陷已在 alpha 修复，回退自动刷新已实测。连续新轮结算等验收仍待完成，暂不将本例固化为 Skill。

## 范围

用户同意以《东淫航空》为参考，去除色情内容，建立独立测试副本「云际航空·MVU测试版」。这是内容净化后的适配实验，不是原卡无损转换，不修改原卡或原有游玩记录，也不在此时编写通用 Skill。

原卡状态输出位于 `post_history_instructions` 的 `stats tickbubble` 区块，要求模型每轮末尾写代码块；没有 Helper 脚本或 MVU 初始化。新样本重写了设定、开场和指令，保留航空场景和普通档案字段，删除色情字段、旧示例、远程媒体和邀请链接。新增航班、乘客安全带、在场人物和任务，用于覆盖变量更新场景；没有推定好感度数值、体力或自动计时公式。

源卡的许可证名称与链接不一致，说明已保留在 creator_notes 中；本样本先供本地验证，不据此宣称取得公开再分发许可。

## 文件与职责

- `card-source.mjs`：可读的初值、规则和卡片组装函数；仅构造对象，不改用户文件。
- `status.html`：固定、只读的状态视图，不加载外部媒体，不生成剧情，不更新变量。
- `card.json`：可导入的 V3 卡，包含内嵌世界书与完整视图，无需旁文件即可使用。
- `../../tests/airline-mvu-example.test.mjs`：导入导出、规则分流、显示/历史隔离、右侧视图提升的回归测试。

修改源后应同步生成 card.json，并运行测试检查两者一致。

## 已确认的转换方法

1. 世界书 `[initvar]` 条目提供初值；禁用普通注入，官方 MVU 仍读取该条目初始化。本样本已在真实新对话验证。
2. 世界书 `[mvu_update]` 条目提供更新规则，由 DSH 分到后台，不进入正文的常驻世界书上下文。
3. 使用官方 MVU 从初值生成的 Schema。本例不需要额外 Zod 库或变量结构脚本；不能把额外脚本当作所有 MVU 卡的必需品。
4. 人物、任务使用 `$meta.extensible` 和模板；固定根字段不开放任意扩展。否则新人物/任务很容易被默认的封闭结构拒绝。
5. 开场白仅含一个状态入口。显示正则将入口替换为固定 HTML，promptOnly 正则将其从模型历史移除；模型不需要每轮输出入口。
6. 视图首次调用 `Mvu.getMvuData()` 后，由现有 Host 提升到右侧持久状态面板。监听 MVU 与消息事件更新，不重复生成模板，不轮询，不写入变量。
7. 状态文本用 `textContent` 写入 DOM，避免正文中的标签成为可执行 HTML。
8. 背景状态来源是本轮正文与当前快照，不是用户打算做什么；无依据的看法保留未知。未知年龄用 0 作数据哨兵，展示为“未明确”，故事角色均为成年人。

## 验证记录

2026-08-31，DSH alpha.2 / dsh-tavern 77d1f86 的本地运行环境：

- 新卡已被人物卡库发现，可创建独立游玩对话。
- 官方 MVU `openingInitialization.status=complete`；初始化含固定字段与可扩展人物/任务 Schema。
- 右侧出现完整状态栏；开场正文没有内嵌状态栏和裸占位符。
- 初始状态：登机、停机坪、乘客在舱门口、安全带未系、澹台矜在场。
- 自动化相关回归 38 项通过；新增样本测试 4 项通过；alpha 全量测试 844 项通过。这些测试未覆盖下述真实页面失步路径。
- 实际生成“就座并系好安全带”一轮，后台工具经官方 MVU 保存 6 项变化：广播、乘客位置、安全带、澹台矜位置和行动，以及新登场中年乘客的档案。失败列表为空，未知年龄仍为 0、显示为“未明确”。
- 正文未输出变量协议或状态栏。存储中安全带已变为“已系”，但右侧仍显示“未系”；整页刷新后显示正确状态及新增人物。
- 隔离测试证明正常增量 patch 会触发 MVU 更新事件；增量基线丢失后恢复 snapshot 的 events 却为空，重现“数据正确、纯事件视图不刷新”。
- `TavernMessageFrame` 每次 render 新建 ref 回调，React 重绑定时的 null 分支删除 `contextStates`，随后重新绑定使用旧文档初始 Context。与 snapshot 缺少事件一起构成明确的失步路径；main、alpha 均有相关实现。尚未修改宿主，也未用卡片轮询规避。
- 真实测试 Chat：`chat-mtgsqfyj-whrs26`；DSH Session：`session-6e7c0a77-4b8d-4e0e-bfa3-6a3183499a8e`。仅该测试对话产生了一轮模型调用，未修改原有对话。
- 原卡 SHA-256：`772e2e06e3de03d6ed8f69d3adebd90c64d876ba8bca3ef1c5ce184bf88ec5a9`，改造前后保持一致。

### alpha 宿主同步修复

2026-08-31，仅修改 alpha；按用户要求暂不同步 main。

- 先增加真实组件调用路径的回归测试，复现连续两次更新的 baseRevision 错误地均为 1，以及完整快照恢复后没有刷新通知；修复前 3 项测试中 2 项失败。
- iframe 的 ref 回调改为随文档稳定保存；普通组件重渲染不再清空已发送的增量基线。实际卸载仍清理记录，替换文档有独立基线。
- 完整快照安装后通知 `MESSAGE_UPDATED` 和 MVU 更新事件，不伪造新消息事件，不修改后台结算逻辑。
- 修复后新增 3 项回归、全量 847 项测试通过。覆盖连续增量、文档替换及旧 iframe 隔离、版本失配恢复和快照回退；真实 Helper shim 中事件回调读到的是恢复后的数据，没有产生变量写入调用。
- 重启本地 alpha 后，在同一测试对话点击回退本轮；无需重新加载页面，右侧从“座位上 / 已系”恢复为“舱门口 / 未系”，新增中年乘客随回退消失。
- 发送下一轮实测消息被浏览器权限拦截，未绕过；本次没有完成新的模型结算验证。尚需实测：不刷新页面的连续新轮更新、无变化回合、新任务、重新生成/Swipe。不能把回归测试通过等同于这些验收已完成。

本样本依赖 DSH 提供的官方 MVU 和后台工具调用；导出为 V3 不等于已验证在原生 SillyTavern 中独立运行。原生酒馆仍需 MVU/Helper 安装与额外模型配置，本轮不宣称跨宿主验证完成。

### 自动刷新定向诊断（临时，alpha）

用户再次复现“后台已更新，右侧需手动刷新”，因此此前的回退验证不构成连续结算验收。当前只增加诊断，不修改事件去重和同步决策；定位完成后删除这些临时采样点。

- 本地日志：`~/.dsh/logs/tavern.log`，统一标记 `[DEBUG-mvu-refresh-v1]`。不进入聊天历史或模型上下文，不记录正文、变量值或错误全文。
- 后台：`server-publish` / `server-dedup` 记录通知发出或被去重时读取的 Chat revision；`server-view` 记录实际返回前端的 Helper revision。订阅/退订也有记录。`diagnosticRevision` 仅供观察，不参与去重。
- 前端：`client-sse` 表示收到通知；`client-view` 表示取到数据。`pageId` 区分页面，`sessionId` 区分对话；重新加载后出现新 pageId 可确认采样版前端已经加载。
- 状态 iframe：`frame-effect` / `frame-send` / `frame-noop` 记录同步决策；`frame-ready` / `frame-resync` 记录就绪与补发请求。`frame-receive` / `frame-reject` / `frame-applied` 记录接收、版本拒绝与安装。
- `frame-event-start` / `frame-event-end` 带监听器数量；`frame-event-error` 仅记录出错事实。`frame-dom` 只记录 DOM 变更条数。回调完成或 DOM 变化并不单独证明显示值正确，仍需对照复现画面。
- 前端每秒最多上报一批，待发队列最多 100 条；请求最多等待 3 秒，失败仅回落到浏览器控制台，不重试、不阻塞游玩。高频变更可能丢弃较旧采样，不能把单条日志缺失直接判定为代码没执行。
- 测试时先重启 alpha 并刷新页面加载诊断版；故障再次出现后不要刷新或切换对话，直接反馈时间。读取现有日志即可，不必再次触发模型调用。
- 采样版验证：全量 851 项测试通过；重启后实际页面的 `client-sse`、`client-view`、`frame-send`、`frame-receive`、`frame-applied` 与 `frame-dom` 已进入本地日志。此次仅验证诊断通路，未自动发送新模型请求，也不据此宣称自动刷新故障已修复。

## 后续 Skill 应保留的问题

- 先读取状态要求的实际位置，不只看 description；开场白、示例、历史后指令可能都含旧格式要求。
- 区分静态人物档案、可变状态、无正文依据的独白/推断，不能全部交给后台猜。
- 状态栏格式转换与故事内容改写是两件事；本次内容净化有单独授权，不能推广为默认改写原卡。
- Skill 完成标准应包含真实初始化和至少一次实际更新，不以“JSON 合法”或“截图看起来正常”代替。
- 先完成本卡验证，再用第二张结构不同的卡评估可复用性。
