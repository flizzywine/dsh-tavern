请协助用户调试刚才引用的游玩回复。先调用 `tavern_read_play_chat` 读取目标轮次概览，再按问题读取模型原文、Session 文本、展示文本或当前人物卡正则诊断；需要核对正则时，再调用 `tavern_read_card_raw` 读取相关 raw 路径。

先解释问题属于模型输出、Session 投影、展示正则还是人物卡自身规则，并给出最小修改方案。只有用户明确确认修改后，才能调用 `tavern_update_card`；不得自动修正人物卡，也不得改写游玩历史。
