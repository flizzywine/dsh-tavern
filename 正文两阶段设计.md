# dsh-tavern 正文两阶段设计（2026-08-16 定稿存档）

> 背景：剧本模式提示词写"仅供参考"，导致初稿完全自由发挥，剧本中大量好的构思未被使用；此前"尽量贴合剧本"又出现正文逻辑前后生硬。定稿后把"内容保真"与"形式整理"拆成两个阶段，各管一件事。

## 问题与结论

- 初稿阶段的问题：提示词把玩家指令当主线、剧本只当参考，LLM 自由发挥，剧本素材流失。
- 润色阶段的问题：润色器被禁止改剧情、动作顺序、段落结构，初稿生硬时润色也修不了。
- 结论：初稿负责"尽量贴合剧本、多装素材"，润色负责"删重复、调顺序、补过渡、润色遣词造句"。

## 1. 初稿阶段：内容保真

- 剧本是剧情主线：**分析剧本，可以进行前瞻，了解后续剧情，尽量贴合剧本发展**。
- 前瞻方式：初稿前可主动调用 `tavern_session action=script` 读后续分块（现有工具，只读）。
- **允许照抄剧本，也允许自由发挥**，不设防照抄条款。
- 允许初稿粗糙：可以生硬、有重复、顺序不顺、略长；这些交给润色阶段整理。
- 玩家指令降为"本轮如何承接/进入剧本"，不再是独立剧情主线。

## 2. 润色阶段：形式整理

- 润色输入：初稿全文 + **上一段正文结尾** + **当前人物姿势 posture** + **本轮对应查看到的剧本块**（prepared 块 + 初稿阶段 peek 过的 lookahead 块）。
- 剧本块的角色是**文风基准**：润色时模仿剧本的遣词造句与语言风格（用词、句式、语感、称呼习惯），允许适当修改和自由发挥，不必照抄；不做禁用其他风格的限制。
- 权力边界放宽，给足够权限：
  - 可以适当删去（重复、生硬、冗余）；
  - 可以适当增加（过渡、衔接、细节，使文字更有质感）；
  - 可以参照剧本适当新增、改写或删除内容；
  - 可以调整顺序（以叙述流畅、逻辑自然为准）；
  - 润色遣词造句。
- 底线：保持剧情主线连贯，不与上一段及已发生情节矛盾。

## 3. 游标与 peek（定稿）

- 游标的作用：**告诉后续阶段"你看哪里"**——即当前应参考剧本的哪个范围，省得 LLM 每轮把整个剧本看一遍。
- 游标的调整者：**候选项模块**。生成候选项时，候选模块评估当前进度，把游标向前、向后或保持不变；正文 commit 不推进游标。
- 正文 context：按游标注入当前参考分块作为起点；LLM 需要更大范围时，用 peek 主动往前往后看，扩大观看范围。
  - 正文阶段 peek = `tavern_session action=script`（scriptOffset 指定任意块号 / scriptQuery 检索）。
  - 候选阶段 peek = `tavern_script_peek` 工具。
- 去掉 `scriptCursor` 参数、`scriptAdvance` 参数与 held 机制（不再锁定"本块未演完"）。

## 改动位置（仅 index.js + persona 文本）

1. `buildSystem` 剧本段：翻转"仅供参考"为"剧本是主线、尽量贴合剧本发展"；增加前瞻指引。
2. context 返回的 scriptLookHint：与初稿前瞻语义对齐。
3. persona 第 3 条 script 流程：同步初稿要求。
4. `polishBody` 提示词：新职责（删重复、调顺序、补过渡、润色；不改变剧情主线与已发生事实）。
5. `polish` action：不再传剧本（styleText），改为传上一段正文结尾。
6. 游标相关：`commitScriptReference`、`normalizeScriptState`、`prepareScriptReference`、工具 schema、context/commit 返回、rollback 回滚。
