# dsh-tavern 使命

## 我们在做什么

dsh-tavern 是一个基于 DeepSeek Harness（DSH）的类 SillyTavern 文字游戏 Agent。

## 为什么基于 DSH

DSH 提供了超高的自由度和完整的 Agent 基础设施。dsh-tavern 应当复用这些能力，把精力集中在文字游戏的业务功能上，不在本项目中重复开发和维护 Agent 基础设施。

## 不复刻 SillyTavern

dsh-tavern 的目标不是复刻 SillyTavern，而是用 Agent 的思路解决提示词工程的本质弊端。

酒馆是提示词工程的产物，因此存在一些固有问题：长对话失忆、人物姿势错乱、输出掉格式。这些问题不应继续依靠堆叠提示词解决，而应通过 Agent 架构解决。

## 文字游戏的第一性原理

**在恰当的地方，注入恰当的上下文和提示词。**

注入过多，模型的注意力会涣散；注入过少，功能会缺失。因此，上下文和提示词必须尽可能少而精确。

每次新增或扩大一段提示词之前，都必须回答：

1. 这段提示词是否必须注入？
2. 如果不注入，是否会导致明确的功能缺失？
3. 它是否可以缩短、延后注入，或改由 Agent 的独立流程处理？

只有确实支撑功能的内容，才应进入模型上下文。

## 不可放弃的核心功能

以下功能是 dsh-tavern 的产品核心，不能作为普通附加功能被删除：

1. **候选项生成**
   这是最核心、绝不可丢弃的功能。

2. **剧本模式**
   LLM 设计和维持长线剧情的能力很差，必须依靠外部剧本支撑故事的长期结构与推进。

3. **对话式 Agent 人物卡新建与编辑**
   人物卡的新建和编辑不是独立于游玩的附属工具，而是游玩的前置准备，不能放弃。
