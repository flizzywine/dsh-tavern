# dsh-tavern 使命

## 我们在做什么

dsh-tavern 是一个基于 DeepSeek Harness（DSH）的类 SillyTavern 文字游戏 Agent。

dsh-tavern 的最终目标，是成为一个好玩、有趣、精彩的文字游戏 Agent。

## 为什么基于 DSH

DSH 提供了超高的自由度和完整的 Agent 基础设施。dsh-tavern 应当复用这些能力，把精力集中在文字游戏的业务功能上，不在本项目中重复开发和维护 Agent 基础设施。

## 不复刻 SillyTavern

dsh-tavern 的目标不是复刻 SillyTavern，而是用 Agent 的思路解决提示词工程的本质弊端。

酒馆是提示词工程的产物，因此存在一些固有问题：长对话失忆、人物姿势错乱、输出掉格式。这些问题不应继续依靠堆叠提示词解决，而应通过 Agent 架构解决。

## 文字游戏的第一性原理

**在恰当的地方，注入恰当的上下文和提示词。**

注入过多，模型的注意力会涣散；注入过少，功能会缺失。因此，上下文和提示词必须尽可能少而精确。

每次新增或扩大一段提示词之前，都必须回答：

1. 这段提示词是否必须注入？
2. 如果不注入，是否会导致明确的功能缺失？
3. 它是否可以缩短、延后注入，或改由 Agent 的独立流程处理？

只有确实支撑功能的内容，才应进入模型上下文。

## 子 Agent 的使用原则

子 Agent 的价值是隔离相互干扰的任务，而不是为了拆分而拆分。决定新增子 Agent 前，必须先回答：**如果不隔离，会发生什么？**只有共用同一个 Agent 会造成明确的上下文污染、目标冲突或输出质量下降时，才应使用子 Agent。

候选项生成需要研究后续行动和剧本走向；如果与正文共用思考过程，容易使尚未发生的内容污染正文，因此需要隔离。状态结算追求严格、稳定的格式；如果交给正文同时完成，既可能掉格式，也会分散正文写作的注意力，因此也适合隔离。

子 Agent 应各自承担单一、明确的职责，其上下文、思考轨迹和输出与正文 Agent 隔离；隔离带来的额外成本必须由明确的质量收益证明。

## 不可放弃的核心功能

以下功能是 dsh-tavern 的产品核心，不能作为普通附加功能被删除：

1. **候选项生成**
   这是最核心、绝不可丢弃的功能。

2. **剧本模式**
   LLM 设计和维持长线剧情的能力很差，必须依靠外部剧本支撑故事的长期结构与推进。

3. **对话式 Agent 人物卡新建与编辑**
   人物卡的新建和编辑不是独立于游玩的附属工具，而是游玩的前置准备，不能放弃。

## 剧本模式的取舍

自由故事模式负责创造故事，剧本模式负责演绎故事。

在剧本模式中，故事的长期方向和关键事件由剧本决定，玩家决定如何经历它们。玩家输入可以改变人物的行动与对白、事件发生的过程、人物关系、情绪、局部场景、叙事节奏以及抵达关键节点的代价，但不能取消剧本走向。

剧本模式遵循：**Guide ＞ 剧本 ＞ 世界一致性 ＞ 本轮玩家指令**。

- **Guide** 是作者层指令，必须遵循。
- **剧本** 是故事的“命运引力”与“空气”：既决定长期走向、关键事件、人物命运、因果关系和伏笔，也影响叙述视角、语言风格、节奏、氛围与遣词造句。新内容应像是从剧本中自然生长出来。
- **世界一致性** 包括人设、人物关系、空间姿势、物理环境、既有事实与因果关系。它负责让剧本自然可信。
- **本轮玩家指令** 可以指定玩家或其他人物的行动、对白、反应以及场景变化。系统应尽可能实现这些局部输入，但必须使其服从更高层约束。

剧本模式唯一的进度变量是**剧本游标**。游标表示下一轮正文默认注入哪个剧本切块：每次正文成功提交后自动前进一块，候选只能通过显式 point 保持或向前跳转。正文与候选 Agent 可以前后阅读任意剧本块，但阅读位置绝不能改变游标。不维护块内完成度、事件清单或第二套进度状态。

提示词是用户可以直接维护和试验的产品内容。用户可能会手动修改提示词；除非用户明确要求恢复、重写或调整，否则任何修复、重构和架构整理都不得把提示词复原成旧版本，也不得用默认内容覆盖用户的修改。

当本轮指令与剧本冲突时，应改变事件发生的路径、人物反应、环境条件或行动结果，使故事继续自然地朝剧本推进，

## 面向用户隐藏复杂性

在宣传和介绍 dsh-tavern 时，应当尽量向用户展示产品能做什么、能够解决什么问题，以及实际的游玩体验，而不是要求用户理解背后的技术实现。

在使用体验上，dsh-tavern 应当尽可能开箱即用。用户不需要经过复杂配置就能开始游玩；技术复杂性和流程复杂性应当由系统内部承担。
