# dsh-tavern TODO

## 当前原则

- v0.5 以 Bug 修复、UI 微调和真实人物卡兼容为主，暂缓扩张功能面。
- 外部兼容 SillyTavern 资源与效果，内部继续使用 DSH 的追加式 Agent 架构。
- 正文、候选、状态和 UI 分离；单个后台任务或展示规则失败不得阻断游玩。

## P0：稳定当前版本

- [x] 修复正则替换中的两位数捕获组：`$10`～`$13` 不得被解析成 `$1` 加数字。
- [ ] 正则只把命中片段的替换产物送入右侧，未命中的正文不得复制过去。
- [ ] 正则累计覆盖整轮正文时撤销展示结果，完整保留正文。
- [ ] 为常见掉格式建立安全回退；无法可靠识别时显示原文，不猜测、不吞正文。
- [ ] 用真实人物卡逐条验证：开场白、宏、HTML、正则、多人物块、重新生成和回退。
- [ ] 修复测试中发现的阻塞 Bug，并完成暗色模式、侧栏、候选框等小幅 UI 调整。

## P1：v0.5 功能

### 世界书动态召回

- [ ] 为大型世界书生成并缓存紧凑目录。
- [ ] 后台 Agent 按当前输入和最近剧情渐进读取条目，生成最少但足够的正文上下文。
- [ ] 保留来源、原因和预算审计；超时或失败时安全回退，不阻止正文。
- [ ] 暂不引入向量数据库，不把完整世界书交给任一 Agent。

### 结构化状态与 UI

- [ ] 后台结算输出地点、时间、人物位置、姿势和必要外观。
- [ ] 状态绑定剧情 revision，并随正文替代和回退恢复。
- [ ] 右侧优先展示已校验状态；正文正则只做兼容展示，不反向覆盖权威状态。
- [ ] 游玩模式自动打开酒馆状态侧栏，卡片模式不自动打开。

### 卡片模式文风 Skill

- [ ] 支持导入、制作、选择、切换和删除文风 Skill。
- [ ] 每次只注入当前选中的一份文风；缺失时回到“无文风”。
- [ ] 文风用于人物卡准备，不直接成为人物卡字段或全局游玩提示词。

### 破甲提示词

- [ ] 预设原样进入预设库，由卡片 Agent 阅读并提取破甲提示词。
- [ ] 破甲提示词以 Markdown 文件保存，侧栏只负责查看、选择和开关。
- [ ] 开启后注入正文、候选、状态结算及其他模型任务。
- [ ] 功能先完成接口；大规模破限验证改用自动化测试，后续单独推进。

### UI 与文档收尾

- [ ] 精简人物卡选择和卡片模式入口，不向用户暴露内部 Workspace 概念。
- [ ] 保持人物卡库、预设库、破甲库和资料库职责分离。
- [ ] 功能稳定后重写 README、安装说明和截图。
- [ ] 验证命令行版、DSH Desktop、更新安装、服务重启和数据保留。

## P2：人物卡专属 UI 工具

正则是兼容层，不能根治横线、块头或字段缺失等掉格式问题。长期方案是在人物卡导入或准备阶段生成并缓存专属 UI Adapter：

- [ ] 分析人物卡的正则、HTML 模板和字段协议，生成结构化 schema 与专属 UI 工具；模板只生成一次。
- [ ] 前台主 Agent 只生成纯正文，不再输出状态块、HTML、分隔符或其他 UI 协议。
- [ ] 正文完成后启动独立 UI 任务，只提交本轮结构化变量，并调用人物卡专属工具。
- [ ] 工具校验参数并确定性渲染 UI；校验失败时返回具体错误并限次重试 UI 任务，不重新生成正文。
- [ ] UI 结果绑定当前剧情 revision；任务失败时保留上一版有效界面，不影响正文、候选和状态结算。
- [ ] 不同人物卡可以使用不同 schema，但统一接入回复展示接口。
- [ ] 后续支持差量更新已有界面，进一步减少 token 和格式错误。
- [ ] 正则在过渡期继续承担旧卡兼容；专属工具稳定后只作为导入时的模板来源，不再参与每轮正文删除和替换。

## P3：酒馆生态兼容

- [ ] 人物卡、世界书和预设：保留原格式，转换为 DSH 上下文、规则与后台任务。
- [ ] EJS 与兼容脚本：在隔离环境中执行，只开放受控数据接口。
- [ ] Tavern Helper、MVU 和轻量 HTML：按真实高频需求逐步桥接，不直接获得宿主权限。
- [ ] 每项能力标明“原生支持、转换支持、沙箱支持或暂不支持”。
- [ ] 使用真实酒馆资源建立回归样本，分别验证可读取、可运行和效果一致。

## 发布门槛

- [ ] 完整测试通过，并覆盖成功、失败、重试和旧数据迁移。
- [ ] Tavern 重启后完成真实浏览器工作流验证，控制台无新增错误。
- [ ] 不破坏自由故事、剧本游标、人物卡原版和历史会话。
- [ ] 新提示词与上下文都有明确用途和预算。
