# 世界书、候选与状态结算共享一个后台 Agent

> 后台 Session 的生命周期与回退方式由 ADR-0003 修订。

正文生成是前台职责；世界书召回、候选生成和状态结算都是基于同一剧情时间线进行理解、但不直接面向玩家的后台任务。三者共享一个分支级持久 Session，减少重复注入和反复研究剧情；任务仍以不同 operation 提交：世界书只提供本轮上下文，候选只能写候选与向前 `point`，结算只能写获准的派生状态。以后增加后台能力时优先增加任务，而不是新增长期 Agent；只有记忆必须隔离或生命周期确实不同，才增加新的 participant。
