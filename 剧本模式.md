# dsh-tavern 剧本模式

## 定位

剧本文件独立于人物卡，是可选项：

- 人物卡仍按原方式导入 PNG/JSON。
- 剧本使用独立的纯文本文件（`.txt` 或 `.md`），在卡片模式中绑定到人物卡。
- 没有剧本文件的人物卡在游玩模式中是自由故事。
- 已绑定剧本的人物卡，在游玩模式新开会话时必须按剧本推进。

## 使用方法

1. 启动 `dsh --profile tavern`，打开 http://127.0.0.1:3081。
2. 左侧切到“卡片”，打开卡片模式选择器：
   - 在“编辑人物卡”子页对目标人物卡点“绑定剧本”（或“替换剧本”），选择独立的 `.txt` 或 `.md` 小说文件。
   - 也可以进入该卡的设定对话，在右侧字段编辑器上方的“剧本绑定”区完成同样操作。
3. 切回“游玩”，点击已绑定剧本的人物卡。新会话自动按剧本推进（有剧本时必须走剧本）；未绑定剧本的卡仍是自由故事。

## 分块与召回

- 导入时按自然段粗分块，目标约 500 字，允许约 350～700 字浮动。
- 不做情节节点、摘要、标签、向量索引或语义预处理。
- **游标只是"当前看哪里"的大致标记**：告诉正文阶段当前应参考剧本的哪个范围，省得整本都看。
- 游标由**候选项模块**在生成候选项时调整：评估当前剧情进度后，可向前、向后或保持不变；正文 commit 不推进游标。
- 每轮正文 context 按游标注入当前参考分块；需要更大范围时，LLM 用 peek 主动往前往后看（`tavern_session action=script`，scriptOffset 指定任意块号）。
- 主正文模型每轮默认只收到游标处的这一块，其余按需 peek，不做强制选块。
- 候选项模块生成候选时可调用 `tavern_script_peek` 前后查看分块，并在输出 JSON 中用 `scriptCursor`（1 起始块号）标记新游标；无效或缺失则游标保持不变。

## 数据

独立剧本保存在：

`/Users/cf/Workspace/dsh-tavern/scripts/<cardId>.json`

剧本文件包含：名称、原文字数、目标分块大小、导入时间和有序分块。人物卡文件本身不写入剧本文本。

每个剧本会话在对应 `chats/*.json` 中保存：

- `scriptState.cursor`：当前参考位置标记（0 起始块号），由候选项模块调整。
- `recalledChunkIds`：曾注入过的分块。
- `skippedChunkIds`：历史遗留字段，新流程不再写入。
- `prepared`：当前轮 context 注入、尚未成功提交的分块。
- `lastReference`：最近一次成功提交的剧本引用。

## 剧本推荐候选

- 剧本会话每轮 assistant 回复后只生成 1 个“剧本推荐”，不再生成自由故事的 5 个候选项。
- 候选项模块能看到上下文（人物卡、现场姿势、最近正文与玩家输入），先阅读游标附近分块，必要时用 `tavern_script_peek` 向前或向后查看剧本。
- 候选必须**有剧情走向意义**：推动剧情进入剧本中的下一处关键场面、冲突或转折，而不只是一个孤立动作；同时从上一段正文结尾自然接起，不重复已发生的动作。
- type 可为 player（玩家行动）/ npc（角色行动）/ scene（结束场景）/ scene2（新场景开始）。
- 生成候选时输出 `scriptCursor` 调整游标（向前、向后或不变）。
- 选“场景结束”后，下一轮剧本推荐固定为 1 个 scene2（新场景开始），且必须与剧本后续内容衔接。

## 当前边界

- 游标不锁定剧情：候选项模块可以向前、向后或保持游标；正文 commit 不推进游标。
- 原文分块进入当前会话上下文，但不会被假设永久保留；长期结果仍由正文历史和故事状态承担。
- 替换某人物卡的剧本后，已有剧本会话会在下一次 context 检测版本变化，并从新剧本开头重置游标、已召回/跳过集合和 prepared 引用。
- context、prepared 与 commit 绑定 DSH 原生 turn；同一 turn 的重复 context 复用同一分块，重复 commit 直接返回已保存结果，不会重复追加消息或推进游标。若未来 DSH 的重试改为创建全新 turn/分支，跨 turn 的外部状态回滚仍需另行适配。
