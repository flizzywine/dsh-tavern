{
  "name": "dsh-profile-tavern",
  "version": "0.2.0",
  "private": true,
  "bin": {
    "dsh-tavern": "bin/dsh-tavern"
  },
  "scripts": {
    "install:tavern": "./bin/dsh-tavern install",
    "test": "node --test tests/*.test.mjs"
  },
  "dependencies": {
    "dsh-codex-connect": "0.1.0-alpha.4.5",
    "dsh-tavern-plugin": "link:./tavern-plugin"
  },
  "dsh": {
    "profile": {
      "bundles": [
        "@deepseek-ai/dsh-base",
        "@deepseek-ai/dsh-web-app",
        "dsh-codex-connect"
      ]
    }
  }
}
