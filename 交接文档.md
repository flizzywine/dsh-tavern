# dsh-tavern 交接文档

> 更新日期：2026-08-16  
> 用途：供新的开发会话直接接手。先读本文，再修改代码。

## 1. 项目定位

`dsh-tavern` 是一个独立的 DSH profile。它不是另起一套聊天程序，而是在正常 DSH WebUI 上做“酒馆极简模式”：保留原生会话、模型选择、输入框、消息流、重试和上下文管理，只替换左侧栏、右侧详情栏，并加入人物卡、故事状态和行动候选。

- 启动命令：`dsh --profile tavern`
- 日常管理命令：`dsh-tavern start|stop|restart|status`
- 地址：`http://127.0.0.1:3081`
- 普通 DSH Web profile 仍使用 3080，两者不要启动在同一端口。
- 当前运行时数据目录就是本文所在目录：`/Users/cf/Workspace/dsh-tavern/`

## 2. 当前产品形态

### 游玩模式（原故事 + 剧本）

1. 左侧只有两个模式：**游玩** / **卡片**。游玩模式下点击“选择人物卡 · 新开游玩”。
2. 选卡后创建正常 DSH 会话，并把人物卡开场白作为第一条原生 assistant 消息写入消息流。
3. 人物卡**已绑定剧本**时，新会话自动按剧本推进（内部 mode=script）；未绑定剧本时按自由故事推进（内部 mode=story）。旧会话保持创建时的内部模式，不会因后来绑定剧本被强行切换。
4. 用户可选择自动生成的行动，也可以在原生输入框自由输入。
5. 模型每轮先调用 `tavern_session(context)` 获取人物卡、历史状态和叙事要求，回复正文后调用 `tavern_session(commit)` 持久化。
6. 单轮小说正文长度由剧情自然决定，不设固定字数限制；可在右侧 Guide 区增删注入上下文的额外指导。
7. 候选项改为手动生成：每轮 assistant 回复后不再自动生成，回复下方统一显示“生成候选项”按钮，点击后独立生成。自由故事（mode=story）生成 5 个：3 个玩家行动、1 个其他角色行动、1 个场景结束；剧本会话（mode=script）只生成 1 个推荐候选（候选项模块可 peek 前后查看剧本，type 可为 player/npc/scene/scene2，面板备注“跟随剧本，只有一个推荐候选项”，并在生成时输出 scriptCursor 调整游标：向前、向后或不变均可）。候选必须从上一段正文结尾先做承接再进入剧本动作，禁止跳接和重复上一段已发生的动作。候选区默认折叠，只显示“接下来的行动 · N 个候选项”，展开后可选择并填入输入框（角色/场景候选自动加「角色行动」「场景结束」前缀标记，模型按标记演绎）。选“场景结束”后正文只做收尾，下一轮候选全部变为“新场景提要”（type scene2，自由故事为 3 个完全不同的新时间/地点/人物组合；剧本会话仍为 1 个且必须与剧本后续衔接）；选一个后，正文的任务是把这段提要完整展开演绎成 250~400 字的小说场景（从提要起点写起、补全被压缩的过程，不把提要当已发生事实、不跳到提要之后的情节）。候选模型偶发空输出/格式错误/JSON 被截断时最多自动重试两次（温度逐步提高），并带从原文抠 choice 对象的兜底解析；三次都失败才报错。
8. 回复下方另有“重新生成正文”：点击后在输入框上方弹出面板，可填可选“指导意见”（例如“写得更长，侧重心理描写”），点“生成并替换正文”即直接替换，无确认步骤；不满意就在新正文下方再点一次。只作用于最新一条正文，仅在上一条玩家输入基础上重新生成。
9. 右侧详情栏：自由故事只显示人物姿势；剧本会话额外显示剧本进度（游标 / 已召回 / 分块总数）。事件、人物、关系等长期信息（lore）已停止维护与注入。
10. 世界书（character_book）：`constant` 条目始终注入；非恒定条目在 `tavern_session(context)` 时由低温检索器用最近几轮正文 + 本轮输入匹配，最多召回 3 条注入本轮；非恒定条目不超过 3 条时直接全部注入，不再额外调用模型。模型检索失败或返回空时，先用条目 keys 对最近正文做关键词兜底匹配，仍无结果则只注入常驻条目，不阻断正文。

剧本分块与召回协议见 `剧本模式.md`（游标由候选项模块调整）；剧本绑定入口已移到卡片模式。

### 卡片模式（原设定 + 抽取，含剧本绑定）

1. 左侧切到“卡片”，点击“＋ 新建 / 编辑人物卡”打开卡片模式选择器。
2. 选择器分两个子页：
   - **编辑人物卡**：可“导入人物卡”（PNG/JSON）补充已有卡；点击列表中的卡后新建设定对话（内部 mode=revision）；每行还可直接“绑定剧本 / 替换剧本 / 解绑剧本”。右侧详情栏变成字段编辑器 + 剧本绑定区；世界书不再是一整块 JSON，而是按条目逐条编辑，每条只显示“名称（触发词）”和“内容”，未展示的 fields（enabled/constant/extensions 等）保存时原样保留。
   - **＋ 从素材新建**：导入独立 txt/md 素材（存 `sources/`，约 500 字粗分块，与人物卡无关），勾选一份或多份后点“开始抽取”会先弹出玩家身份输入框，确认“谁是玩家（{{user}}）”；留空则在抽取对话中由助手先追问确认。随后创建抽取会话（内部 mode=extract）。
3. 仍复用 DSH 原生主对话流，不使用独立的人物卡工作室聊天框。
4. 设定对话：模型与用户讨论人物卡；只有用户明确确认修改时才提交最小字段 patch；右侧字段编辑器可直接编辑并保存。
5. 素材抽取：模型按游标每轮注入最多 6 块素材，与用户讨论、提问并提炼人物卡草稿；玩家身份（{{user}}）是抽取会话的独立必填状态，由开始抽取时的输入框或对话中 commit 的 `{"player":"..."}` 确认，右侧详情栏单独显示。生成 system_prompt / post_history_instructions 时角色必须用第三人称，禁止“你是{{char}}”。用户确认修改时通过 commit 的 draftPatch 更新草稿（字段同人物卡）。“保存为新人物卡”要求角色名和玩家身份都已确认，并把玩家身份写入 creator_notes；随后可在游玩模式选卡开新故事。
6. 卡片会话不生成剧情候选项、不结算姿势；删除素材后既有抽取会话自动跳过缺失素材。
7. 设定对话与抽取会话在“卡片历史”中合并显示，按类型打标（设定 / 抽取）；抽取会话在定稿前没有人物卡，定稿后自动关联新卡。

### 会话管理

- 酒馆 profile 左侧只显示酒馆会话，不显示其他 profile 的会话。
- 游玩历史（自由故事 + 剧本）与卡片历史（设定 + 抽取）分别过滤。
- 每条历史会话支持重命名、删除。
- 删除时同时归档 DSH 原生会话并删除酒馆 chat 数据。

## 3. 关键路径

| 内容 | 绝对路径 |
|---|---|
| Tavern profile 配置 | `/Users/cf/.dsh/profiles/tavern/cordis.patch.yml` |
| Tavern profile 依赖 | `/Users/cf/.dsh/profiles/tavern/package.json` |
| 插件宿主端 | `/Users/cf/.dsh/profiles/tavern/tavern-plugin/lib/index.js` |
| 插件客户端 | `/Users/cf/.dsh/profiles/tavern/tavern-plugin/lib/client.js` |
| 插件清单 | `/Users/cf/.dsh/profiles/tavern/tavern-plugin/package.json` |
| Tavern preset 运行时副本（插件自动同步，勿手动维护） | `/Users/cf/.dsh/.agent-presets/tavern/agent.cordis.yml` |
| Tavern profile 内 preset 唯一维护副本 | `/Users/cf/.dsh/profiles/tavern/presets/tavern/agent.cordis.yml` |
| 数据索引 | `/Users/cf/Workspace/dsh-tavern/index.json` |
| DSH session → Tavern chat 映射 | `/Users/cf/Workspace/dsh-tavern/sessions.json` |
| 人物卡 | `/Users/cf/Workspace/dsh-tavern/cards/` |
| 酒馆聊天与状态 | `/Users/cf/Workspace/dsh-tavern/chats/` |
| 酒馆设置 | `/Users/cf/Workspace/dsh-tavern/settings.json` |
| 独立剧本文件 | `/Users/cf/Workspace/dsh-tavern/scripts/<cardId>.json` |
| 抽取素材 | `/Users/cf/Workspace/dsh-tavern/sources/<sourceId>.json` |
| 剧本模式设计说明 | `/Users/cf/Workspace/dsh-tavern/剧本模式.md` |
| 服务管理命令 | `/Users/cf/.local/bin/dsh-tavern` |

插件实体已完全放入 `profiles/tavern/tavern-plugin`，Tavern profile 通过 `link:./tavern-plugin` 加载。DSH 启动器会在 profile overlay 之后强制重写 preset 的 shipped root（profile 声明的额外 `roots` 不生效），只有 `~/.dsh/.agent-presets` 用户 root 会保留；因此酒馆插件在宿主端启动时自动把 profile 内的 `presets/tavern/` 同步到 `~/.dsh/.agent-presets/tavern/`，并在新建会话前再次自愈同步。profile 内副本是唯一维护源，修改 persona 只改这里再重启；用户目录里的运行时副本不要手动编辑或删除。不要把插件依赖改成目录副本，否则很容易出现“改了源码却加载旧代码”。

## 4. 架构与数据流

### 宿主端 `index.js`

- 从当前 workspace root 派生 `dsh-tavern/` 数据目录。
- 提供同源 HTTP RPC：`POST /api/dsh-tavern/<method>`。
- 管理人物卡导入、复制、修改、删除以及 chat、状态、候选项和 session 映射。
- 管理独立可选剧本文件：导入时约 500 字粗分块；剧本会话维护游标、已召回集合和本轮 prepared 引用。游标是"当前看哪里"的标记，由候选项模块生成候选时调整（向前/向后/不变），正文 commit 不推进；正文按游标注入参考块，可用 action=script peek 前后扩大范围。`startChat()` 在人物卡已绑定剧本时强制使用 `script` 内部模式；同一大模式（游玩/卡片）内复用当前会话。
- 注册模型工具：
  - `tavern_session`：正式流程使用；支持 `context`、`commit`。`context` 必须传本轮 `userText`；剧本类会话会在这里完成分块选择与注入。
  - `tavern_probe`：仅调试，不应让正式 preset 调用。
- 插件内部模型选择优先级：`settings.json` 的 provider/model 覆盖 → 当前 DSH 会话模型 → DSH 全局默认模型。各 profile 必须能从共享 credentials service 或自身启动环境读取对应 provider 的凭据；不要在 Tavern 客户端强制改写用户模型选择。
- `openai-codex` 调用不会传 `temperature`，避免 Responses API 参数报错。
- 故事提示词由 `buildSystem()` 组装，单轮长度约束也在这里。

### 客户端 `client.js`

主要注入槽：

- `sidebar`：游玩/卡片两大模式、人物卡选择、卡片模式内剧本绑定与素材抽取、历史会话、重命名和删除。内部仍使用 `story/script/revision/extract` 四种 chat mode，侧栏按 `groupOfMode()` 归并显示。
- `details`：游玩状态栏（剧本会话含游标进度）、卡片模式的字段编辑器（含剧本绑定区）或素材抽取面板。
- `conversation.chat.assistant-actions`：回复下方“重新生成”候选按钮。
- `conversation.input.dock`：输入框上方的候选行动区。

候选面板通过模块内 `candidatePanel` 小型状态容器在 assistant action 与 input dock 之间同步。候选生成后默认折叠。

### 开场白与原生消息流

`appendNativeOpening()` 会把 `first_mes` 写成 DSH 原生 assistant 消息，而不是额外绘制一块自定义 UI。这里有一个关键修复：写入合成的 turn 1 后，必须同步推进 idle agent 的 `phase.lastTurn`。否则用户第一次真实输入也会被写成 turn 1，第二轮回复会叠在第一轮输入上，开场白也可能看似消失。

不要删除以下逻辑：

```js
if (agent.phase !== undefined && agent.phase !== null && agent.phase.kind === 'idle') {
  agent.phase.lastTurn = Math.max(Number(agent.phase.lastTurn) || 0, turn)
}
```

## 5. 人物卡与状态

支持 SillyTavern JSON，以及带 `chara`/`ccv3` 文本块的 PNG 人物卡。核心字段包括：

- `name`
- `description`
- `personality`
- `scenario`
- `first_mes`
- `alternate_greetings`
- `mes_example`
- `system_prompt`
- `post_history_instructions`
- `creator_notes`
- `tags`
- `character_book`

故事状态保存在对应 `chats/*.json`：

- `posture`：当前人物动作、位置、姿态等即时状态，每轮结算覆盖更新，是唯一持续维护并注入系统的字段。
- `lore`：历史遗留的人物、关系、事件等长期事实。2026-08 起不再结算、不再注入、不再在右侧栏显示；旧数据保留在文件中，仅兼容旧 UI（已废弃）与编辑 RPC，不做任何更新。
- `candidates`：与最新 assistant messageId 绑定的三个行动候选。
- `scriptState`（仅剧本会话）：游标、已召回/跳过分块、prepared 引用和最近引用。
- 重新生成正文只作用于最新一条正文；剧本模式下重生成不重新注入剧本分块（按普通故事续写处理）。
- 替换机制（DSH 消息流只增不改，`surfaceOp: replace` 只作用于模型面、界面不渲染）：宿主先以合成 turn 把新正文 append 进原生流（可见），再用 replace 事件遮蔽[旧正文…新追加]区间，模型上下文只剩一份新正文；插件 chat.messages 同步原地替换。旧 turn 的正文由客户端隐藏（display:none，隐藏轮次记入 localStorage，刷新后仍隐藏）。模型每轮上下文由 `session.deriveMessages()` 从 surface 派生，不读原始日志，旧文不会污染后续生成。

这些 JSON 含用户实际人物卡和剧情内容，调试时不要覆盖、清空或提交到公共仓库。

## 6. 已完成的重要修复

- 独立 Tavern profile，端口 3081；普通 web profile 不被酒馆自动接管。
- 新建会话先选人物卡，历史会话直接列表。
- 新建游玩/卡片会话后会真正进入对应 DSH 会话。
- 四模式（故事/剧本/设定/抽取）重构为两个 UI 模式：游玩模式（故事+剧本，有剧本必须走剧本）与卡片模式（设定编辑+素材抽取+剧本绑定），旧会话按内部 mode 原样映射。
- 开场白改成原生 assistant 输出，旧开场白大卡片 UI 已废弃。
- 修复合成 turn 1 与真实 turn 1 冲突导致的消息叠层问题。
- 候选项改为手动点击“生成候选项”后生成，可自由输入，默认折叠；剧本会话只生成 1 个推荐候选。
- 右侧故事状态栏和设定字段栏。
- 会话重命名、删除。
- 故事正文长度从 `150~600 字` 改为 `约 150 字`，后又调整为 `约 300 字`，最终取消固定字数限制。
- 新增可选剧本模式：人物卡与剧本文件分离，约 500 字粗分块；游标由候选项模块生成候选时调整（向前/向后/不变），正文 context 按游标注入参考块，并可 peek 前后扩大范围。
- 故事状态改为只维护人物姿势：结算器只输出 `{"posture":"..."}`，不再整合事件/人物/关系等 lore；系统提示、剧本分块选择提示与右侧详情栏均不再注入或显示 lore，旧 lore 数据原样保留在 chat 文件中。
- 候选分为玩家行动/角色行动/场景切换三类并带前缀标记，正文按“导演指令”规则理解；叙事要求明确“指令≠已完成事实”：用户输入是分镜指令，正文必须完整复述、重新演绎指令中的动作与台词，并把它作为正文开头的一部分；演完指令后可以且应该继续自然续写人物反应与后续发展，不再限制“最后一句必须落在指令末尾”，只禁止跳过指令或把指令写成过去状态。
- 结算任务完成后会重新读取最新 chat 再写姿势；若模型调用期间出现了新提交，会继续结算，避免旧快照覆盖新消息或剧本游标。
- 修复 Tavern preset 实际未被启动器发现的问题：profile 内 `presets/tavern/` 为唯一维护副本，插件启动/新建会话时自动同步到 `~/.dsh/.agent-presets/tavern`；普通 web profile 禁用 user preset root。
- `openai-codex` provider 配置与 temperature 兼容处理。
- dsh-web-ui-all、at-files 已在先前配置中禁用，以减少 UI/功能冲突。
- 卡片模式右侧世界书从整块 JSON 改为逐条编辑：每条只显示名称（触发词）与内容，其余字段隐藏但保存时原样保留。
- 剧本会话候选项改为唯一“剧本推荐”：候选项模块可 peek 前后查看剧本，生成一条 player/npc/scene/scene2 路线候选，并输出 scriptCursor 调整游标（可向前/向后/不变）；UI 标题、重生成按钮与意见面板同步按剧本模式显示。
- 素材抽取新增玩家身份确认：开始抽取时先弹窗询问“谁是玩家（{{user}}）”，抽取系统提示把玩家身份作为最高优先级，commit 可提交 `{"player":"..."}`，保存新卡前必须已确认玩家且 system_prompt 不得使用“你是角色”式第二人称。
- 剧本推荐与剧本正文增加连贯性要求：候选和正文都必须从上一段正文结尾自然承接，先补过渡再进入剧本动作，禁止跳接和重复上一段已完成动作。
- 游玩模式新增“回退本轮”：删除最近一次用户输入与 LLM 输出，同时回退 posture、awaitingScene、剧本游标/已召回/跳过集合和 nativeCommits；原生 surface 用空消息 replace 本轮区间，客户端隐藏对应 turn tail 及用户输入。
- 卡片模式（设定对话）新增只读剧本工具：`tavern_session action=script`，仅 revision 会话可读取当前卡绑定剧本，scriptQuery 按关键词检索或 scriptOffset 按块号读取，每次 1~6 块；不暴露 Bash/文件系统。
- 游标语义改为"当前看哪里"的标记：去掉 commit 侧 `scriptCursor`/`scriptAdvance` 参数与 held 机制，正文 context 直接按游标注入当前参考块；游标由候选项模块生成候选时输出 `scriptCursor` 自由调整（向前/向后/不变），正文 commit 不推进。

## 7. 运行与验证

修改宿主端或客户端后：

1. 运行 `dsh-tavern restart`。命令会先确认 3081 的监听进程确实是 `dsh --profile tavern`，避免误杀其他服务；正常停止超时后只报错，不强杀进程。
2. 在浏览器打开 `http://127.0.0.1:3081`，并按 `Cmd + Shift + R` 强制刷新。仅重启宿主不会替换旧标签页内存中的客户端代码。
3. 用 `dsh-tavern status` 确认服务状态；启动失败时查看 `~/.dsh/logs/tavern.log`。

首次手动启动仍可使用 `dsh --profile tavern`。完整管理命令如下：

```bash
dsh-tavern status
dsh-tavern restart
dsh-tavern stop
dsh-tavern start
```

基础检查：

```bash
node --check /Users/cf/.dsh/profiles/tavern/tavern-plugin/lib/index.js
node --check /Users/cf/.dsh/profiles/tavern/tavern-plugin/lib/client.js
curl -s -X POST -H 'Content-Type: application/json' -d '{}' \
  http://127.0.0.1:3081/api/dsh-tavern/listCards
```

UI 回归至少覆盖：

1. 游玩模式 → 选人物卡 → 进入会话 → 开场白位于消息流第一条；未绑定剧本的卡创建为自由故事，已绑定剧本的卡自动创建为剧本会话。
2. 发送一条消息 → 顺序必须是开场白、用户输入、助手输出，不能重叠。
3. 助手输出结束 → 候选项自动生成且默认折叠 → 展开、选择、填入输入框均正常；回复下方“重新生成候选项”打开意见面板，填/不填意见重生成均正常（行动候选与场景候选都验证一次）；“重新生成正文”同样可用。剧本会话同位置验证“剧本推荐”：只有 1 个候选、自动选中，标题/重生成按钮为剧本模式文案，类型四类逐一覆盖。
3.1 点“重新生成正文” → 面板出现 → 填/不填指导意见 → “生成并替换正文” → 旧正文消息被隐藏、新正文出现在消息流末尾，右侧姿势重新结算；刷新页面后旧正文仍保持隐藏。
4. 用户不选候选，直接自由输入也正常。
5. 新建会话后右侧状态栏出现；偶发未出现时刷新会恢复，目前用户决定暂不修改。
6. 切到卡片模式 → 编辑人物卡子页：为某卡绑定/替换/解绑剧本、进入设定对话后右侧字段编辑器和剧本绑定区正常、保存字段生效；世界书以条目列表展示（每条仅名称/内容），添加、删除、编辑后保存，未展示字段原样保留。
7. 游玩模式重新选该卡 → 自动进入剧本会话；右侧显示剧本进度；发送一条消息 → context 注入游标处参考块 → 正文生成；点击“生成候选项” → 候选项模块输出候选并按 scriptCursor 调整游标（验证向前、向后、不变三种情况），已召回数增加。
8. 卡片模式 → “＋ 从素材新建”子页：导入 txt/md 素材 → 勾选素材新建抽取会话 → 对话中确认修改后右侧草稿更新 → 素材进度推进 → “保存为新人物卡”后游玩模式出现新卡。
9. 卡片历史合并显示设定/抽取会话并带类型标签；游玩历史合并显示故事/剧本会话；重命名、删除均正常。

## 8. 已知边界与后续方向

- 新建会话/切换模式/打开历史会话后，右侧状态栏通过有限次重试（每次间隔约 120~200ms，共 6~10 次）确保打开，解决“卡片模式选卡后右侧栏不出现”的问题；若仍偶发，研究 DSH 切换 session 时对 details layout 的重置时机。
- 绑定/解绑剧本只影响之后新建的游玩会话；已有会话保持创建时的内部模式（旧自由故事不会自动变成剧本）。
- `client.js` 仍保留早期自定义整页 UI/人物卡工作室的一些旧组件（如 `TavernWindow`、旧 CardStudio 相关代码），当前主路径没有使用。清理前必须先确认 slot 注册和引用，避免误删仍被复用的字段组件。
- 数据目录跟随 workspace root；换 workspace 可能得到另一套 `dsh-tavern/` 数据。
- 候选生成与剧情输出是额外模型调用，仍需关注延迟和费用；剧本会话正文 context 不再额外调用分块选择器，需要的范围由 LLM 按需 peek。
- 剧本是线性一次性参考流：每轮成功 commit 后即消费，不保证原文事件完整演出；这是当前产品设计，不要擅自改成剧情节点状态机。
- 工具流程通过 `nativeCommits` 绑定 DSH 原生 turn：同一 turn 重复 context 会复用引用，重复 commit 不再追加消息、重复 patch 或推进游标。若未来 DSH 把重试实现为全新 turn/分支，跨 turn 的外部状态回滚仍应按分支/重放事件专门适配。
- 替换剧本后，该人物卡已有剧本会话会在下一次 context 检测版本变化并从新剧本开头重置；不尝试把旧游标迁移到新文本。
- PNG 卡兼容性应继续拿真实 V2/V3 卡回归。
- DSH 升级可能改变 slots、session event 或 profile bundle，升级后重点检查侧栏、详情栏和原生开场白写入。

## 9. 常见错误

| 错误 | 处理 |
|---|---|
| `session create failed: agent-preset-not-found: preset "tavern" not found` | 运行时副本缺失且自愈同步未执行成功；确认 `/Users/cf/.dsh/profiles/tavern/presets/tavern/` 两个文件齐全后重启 Tavern profile（插件会自动重建 `~/.dsh/.agent-presets/tavern/`）。 |
| `EADDRINUSE 127.0.0.1:3080/3081` | 已有 DSH 进程占用端口；先运行 `dsh-tavern status`，Tavern 服务使用 `dsh-tavern restart`，不要重复启动。 |
| `Provider is not configured: openai-codex` | 检查 `dsh-codex-connect` 是否仍在 Tavern profile bundles/dependencies，重新登录 Codex。 |
| `Unsupported parameter: temperature` | 确认 `callModel()` 对 `openai-codex` 不传 temperature。 |
| `Failed to load plugins ... keyed slot ... already has an entry` | 两个 UI 插件注册了同一 keyed slot；调整 priority 或禁用冲突插件。 |
| 页面改动没生效 | 运行 `dsh-tavern restart`，再按 `Cmd + Shift + R` 强制刷新浏览器；普通刷新或只重启宿主可能仍保留旧客户端。 |
| 开场白消失、回复骑在输入上 | 检查 turn 号是否重复，并确认 `appendNativeOpening()` 保留 `phase.lastTurn` 修复。 |

## 10. 建议技能

- `playwright`：修改 UI 后做真实浏览器回归、截图和 DOM 检查。
- `browser:control-in-app-browser`：需要复用应用内已登录浏览器状态时使用。
- `character-card-grill`：设计、审查或修订人物卡字段与设定对话流程时使用。
- `handoff`：下一次大阶段结束后更新本文。

## 11. 接手原则

1. 优先复用 DSH 原生 UI 和 session，不再建立一套平行聊天系统。
2. 游玩与卡片两大模式共享主界面，但历史和右侧栏语义分开；剧本文件始终独立于人物卡且可选，绑定入口在卡片模式。
3. 先保护用户已有 `cards/`、`chats/`、`sessions.json`，再改逻辑。
4. 每次 UI 改动都验证消息顺序、候选折叠、自由输入和右侧栏。
5. 修改后更新本文中“当前产品形态”“已知边界”和验证清单。
